---
description: 运行测试与 CI 要求
alwaysApply: true
---

# 测试与 CI

- **改完代码后必须能通过测试**：在提交或合并前运行 `pnpm test` 和 `pnpm test:e2e`（E2E 需无其他进程占用 3000 端口）。
- **单元测试**：业务逻辑、数据转换、工具函数用 Vitest 覆盖，命令 `pnpm test`。
- **E2E 测试**：核心用户路径用 Playwright，命令 `pnpm test:e2e`；CI 中可设 `CI=1` 自动启动 dev server。
- **Lint**：提交前运行 `pnpm lint`，保证通过。
- 新增功能时同步补充或更新相关单元测试或 E2E，避免测试变红。
